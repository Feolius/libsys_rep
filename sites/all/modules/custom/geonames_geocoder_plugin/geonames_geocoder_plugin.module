<?php

/**
 * Implementation of hook_ctools_plugin_dierctory() to let the system know
 * we implement plugins.
 */
function geonames_geocoder_plugin_ctools_plugin_directory($module, $plugin_type) {
  return 'plugins/' . $plugin_type;
}
