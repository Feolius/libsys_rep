<?php

/**
 * Implements hook_menu().
 */
function library_admin_menu() {
  $items = array();
  $items['admin/library_settings'] = array(
    'title' => t('Site configuration page'),
    'help' => 'This page is contain all settings for site',
    //'page callback' => 'drupal_get_form',
    //'page arguments' => array('nodehierarchy_admin_settings'),
    'access arguments' => array('administer site configuration'),
    'type' => MENU_NORMAL_ITEM,
  );
  return $items;
}

function library_admin_menu_alter(&$items) {
//kpr($items);die;
}
